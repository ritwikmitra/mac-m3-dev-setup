services:
  redis:
    image: redis:latest
    container_name: dev-redis
    restart: "no"
    ports:
      - "6379:6379"
    volumes:
      - /docker/db/redis:/data
    command: ["redis-server", "--appendonly", "yes"]
