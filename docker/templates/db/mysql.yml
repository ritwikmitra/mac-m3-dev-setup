services:
  mysql:
    image: mysql:latest
    container_name: dev-mysql
    restart: "no"
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_USER: dev
      MYSQL_PASSWORD: dev
      MYSQL_DATABASE: dev
    ports:
      - "3306:3306"
    volumes:
      - /docker/db/mysql:/var/lib/mysql
